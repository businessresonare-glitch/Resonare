# Job photos

Drop the five site photographs here. The filenames are already wired into the
gallery in `../../index.html`:

| File | Shows |
|---|---|
| `01-culvert-road.jpg` | Backhoe laying box culverts along the village road |
| `02-switchboard.jpg` | 415V switchboard — ammeter, voltmeter, indicator lamps |
| `03-db-board.jpg` | DB board with the rows of MCBs |
| `04-control-panel.jpg` | Machine cabinet with the PLC and green VFD drives |
| `05-trench.jpg` | Open service trench beside the house |

About 1600px wide, JPEG, under ~400KB each.

Until a file is here the tile shows a labelled slot instead — `site.js` catches
the load error and marks it, so a missing photo never renders as a broken
image. Nothing needs changing in the markup when you add them.
