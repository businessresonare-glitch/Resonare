{
  "name": "resonare",
  "version": "2.0.0",
  "private": true,
  "description": "Marketing site for RESONARE. Static HTML/CSS/JS; the only built file is assets/world.js.",
  "scripts": {
    "build:world": "esbuild src/world/boot.js --bundle --format=iife --minify --target=es2019 --outfile=assets/world.js",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "three": "^0.169.0"
  }
}
